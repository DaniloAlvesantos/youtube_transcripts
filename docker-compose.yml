version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: youtube-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: youtube

volumes:
  mongo_data:
